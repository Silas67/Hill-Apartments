import PageHero from "../PageHero";

const Hero = () => (
  <PageHero
    title="Services"
    lede="From the first viewing to the final handover, and every year of ownership after it."
  />
);

export default Hero;
